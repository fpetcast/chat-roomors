const chatroomSelectionForm = document.getElementById('chat-selection');
console.log(chatroomSelectionForm);

//send the user data to backend to create a session cookie 
// chatroomSelectionForm.addEventListener('submit', (e) => {
//     e.preventDefault();

//     let username = e.target.elements.username.value;
//     let room = e.target.elements.room.value;

//     let data = {
//         username: username,
//         room: room,
//     }
// })

