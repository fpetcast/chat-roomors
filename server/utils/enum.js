const BOTNAME = 'Roomor Bot'

//room events
const JOIN = 'join';
const LEFT = 'left';

//meta enums
const METACODE_ERR = 'meta_err'
const METAVOTE = 'vote';

module.exports = {
    JOIN,
    LEFT,
    BOTNAME,
    METACODE_ERR,
    METAVOTE
}